<h1><%= title @category.name.titlecase + " Furniture" %></h1>
<div class="row">
  <% @products.each do |product| %>
    <%= render "/product", product: product %>
  <% end %>
</div>
<%= render "/pagination", model: @products %>
